@model IEnumerable<Catogery>
@{
    ViewData["Title"] = "Index";
}

<div class="container p-3">
    <div class="row pt-4">

        <div class ="col-6">
            <h2 class="text-primary"> Catogery List</h2>
        </div>
        <div class ="col-6 text-right" >
            <a asp-controller="Catogery" asp-action="create" class="btn btn-primary">
                  creat new catogery
            </a>
        </div>
    </div>
    <br/>
    <br/>
    @if (Model.Count() > 0)
    {
        <table class="table tab-content table-striped" style="width:100%">
            <thead>
                <tr>
                    <th>Catogery Name</th>
                    <th>Catogery Order</th>
                    <th>

                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach(var item in Model)
                {
                    <tr>
                    <td style="width:50%">@item.Name</td>
                    <td style="width:50%">@item.displayOrder</td>
                    <td class="text-center">
                        <div class="w-75 btn-group" role="group">

                        <a asp-controller="Catogery" asp-action="Edit" class="btn btn-primary" asp-route-id="@item.Id">
                              Edit
                        </a>
                        <a asp-controller="Catogery" asp-action="Delete" class="btn btn-danger" asp-route-id="@item.Id">
                            Delete
                        </a>

                        </div>
                    </td>
                    </tr>
                }


            </tbody>
        </table>
    }

</div>
